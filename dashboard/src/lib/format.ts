/*
 * A1.5 — Formateurs purs (Intl fr-FR), testés dans format.test.ts.
 *
 * À écrire ici :
 *   - export function formatEuros(cents: number): string      → "24,90 €"
 *   - export function formatDateFr(iso: string): string
 *   - export function formatSlot(slot: { date; start; end }): string → "lun. 8 sept., 09:00–11:00"
 *
 * Dans les tests, normaliser les espaces insécables : .replace(/\s/g, " ").
 */
export {};
