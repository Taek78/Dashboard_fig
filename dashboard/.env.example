# Copier en .env.local (ignoré par git) et remplir. Inutile pendant la piste A (fixtures).
# Piste B1 : DATA_SOURCE=mock|db et DATABASE_URL (voir docs/a1-spec-branchements.md § 1.d)
DATABASE_URL=postgresql://fig:fig@localhost:5432/fig
# A7 : générer avec `npx auth secret` (32 caractères minimum). Ne jamais commiter la valeur.
AUTH_SECRET=
