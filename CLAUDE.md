# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Contexte

Dashboard d'administration réalisé pour un **client réel** : l'application **FIG**, un service de livraison de fruits et légumes. Les données du client sont celles de vraies personnes, une erreur en production a un coût pour son équipe, et l'auteur engage sa responsabilité de sous-traitant. Le périmètre reste celui du produit : pas de sur-architecture, chaque séparation doit avoir un pourquoi en une phrase. Projet indépendant de la boutique personnelle FIG de Zaki (`Taek78/FIG_nextJs`) : ne rien en réutiliser.

Décisions client : la base de données n'existait pas, **le dashboard la crée et la possède** (schéma dans `src/db/schema.ts`, migrations dans `drizzle/`). L'application FIG s'y branche par l'**API du dashboard** (`/api/v1`, décision du 2026-09-17, `docs/api.md`), jamais en SQL direct. Utilisateurs : l'équipe du client, quatre rôles (admin, gestionnaire, lecture, livreur).

État : produit complet, sur PostgreSQL seul (développement, tests, CI) ; reste ce qui dépend du client (`docs/backlog.md`). Toujours : aucune donnée personnelle réelle sur un poste, aucun déploiement sans les gardes de production, jamais de migration ou de seed vers une base qui n'est pas la nôtre sans l'avoir dit.

Documentation : `README.md` (mise en route), `docs/architecture.md` (arborescence, rôle de chaque couche, flux), `docs/base-de-donnees.md`, `docs/backlog.md`, `docs/glossaire.md` (tout terme nouveau employé avec l'auteur doit y être ajouté), `docs/branchements.md`, `docs/rgpd.md` (données personnelles : inventaire, registre, durées, procédures d'accès et d'effacement, violation).

## Mode de travail

L'auteur est un développeur junior autodidacte, francophone, qui progresse vite. Claude code lui-même (demande du 2026-09-13), en expliquant le pourquoi de chaque choix en une ou deux phrases, sans analogies, sans questions de compréhension, sans exercices. Il maîtrise App Router, Server Actions, zod, TypeScript strict, Tailwind ; expliquer brièvement la première fois : shadcn base-nova, `server-only`, `useActionState`, Drizzle, Auth.js, Playwright.

- Claude écrit et maintient **tous les tests** (`dashboard/test/` en miroir de `src/`, `dashboard/e2e/`) à chaque fonctionnalité livrée ou corrigée.
- Vérification rituelle avant de considérer une tâche terminée : `npm run db:test` (Docker Desktop lancé) puis `npm run check` ; pour ce qui touche aux écrans, `npm run build` puis `npm run test:e2e`. Signaler les erreurs sans les comptabiliser.
- Le fonctionnement sécurisé et visible passe avant le style, sauf demande ponctuelle.
- **Ne jamais commiter ni pousser sans demande explicite.** Commits conventionnels en français (`feat:`, `fix:`, `docs:`, `chore:`), jamais `.env.local` ni un secret.
- Ne pas installer, supprimer ou mettre à jour une dépendance sans accord. Ne jamais modifier un fichier qui n'a pas été lu.

## Équipe d'agents

Quatre sous-agents en lecture seule dans `.claude/agents/`, orchestrés par `/equipe <demande>` : **tech-lead** (cadre, découpe, arbitre : sécurité > fonctionnement > lisibilité > style), **ui-ux-designer** (écrans shadcn et tokens de `globals.css`), **mentor-reviewer** (relecture pédagogique, lance `npm run check`), **security-backend-architect** (Drizzle, Server Actions, RBAC, zod, Auth.js).

## Structure

Une seule app Next.js dans `dashboard/` ; toutes les commandes npm s'y lancent. Détail complet dans `docs/architecture.md`. L'essentiel :

- `src/domain/<domaine>/` : `types.ts`, `status.ts` ou `category.ts` (`as const` + libellés français), `rules.ts` (pur), `schemas.ts` (zod des **entrées** seulement), `fixtures.ts` (déterministes, sans personne réelle ; pour les commandes et les clients, `scenario.ts` écrit à la main autour du 7 septembre 2026 et `orders/history.ts` génère deux ans d'historique réaliste à graine fixe, la fenêtre du 5 au 9 septembre 2026 restant au scénario ; les tests qui raisonnent sur des valeurs précises importent `scenarioOrders` / `scenarioCustomers`), `source.ts` (contrat). Domaines : `orders` (+ `assignment.ts`, `discount.ts` avec `bestDiscount`, `delivery-fee.ts` barème des frais, `slot.ts` créneau d'une heure entre 10:00 et 20:00), `deliveries` (jour de Paris, raccourcis des derniers jours, avancement segmenté, itinéraire ; la section Livraisons a été fondue dans Commandes le 2026-09-16), `products`, `customers` (+ `loyalty.ts` compteur cumulé, `tier.ts` catégorie basique / fidèle et historique déduits des commandes, `consents.ts` trois autorisations, `referral.ts` code « Nom#0000 » et `signupStats` (métrique parrainages), `client-type.ts`, `directory.ts` tris dans les deux sens), `communities` (lecture seule ; `kind.ts` trois types voisinage / entreprise / point relais et visibilité public (intégration directe) / privé (sur invitation), jamais vides, affichés par `CommunityBanner` en tête de carte et de fiche ; `discount.ts` taux selon le nombre de membres), `notifications` (file des notifications d'état : `rules.ts` texte, `source.ts` lectures, `fixtures.ts`), `staff` (personnel ; `isPresent`, `unavailableRoles` pour l'alerte du tableau de bord), `articles`, `metrics`, `engagement` (stores : téléchargements, inscriptions, note ; plus de réclamations depuis 0010), `auth` (+ `password-policy.ts` règles pures de la politique et jauge, `common-passwords.ts`, `tokens.ts` sortes et validités des jetons, `mails.ts` textes des mails), `mail` (contrat `MailSender`, type `MailMessage`), `messages` (boîte de réception « Nous contacter » : `subject.ts` les six objets et `CLAIM_SUBJECTS` les quatre réclamations, `status.ts` non traité / traité, `attachment.ts` liste blanche de formats et limite de dix, `countComplaints`), `privacy` (RGPD : `retention.ts` durées de conservation et `isCustomerInactive`, `anonymization.ts`, `export.ts`). Règle commune des dates « du / au » dans `src/lib/days.ts` (`readDateRange`) : une seule date = ce jour-là, dates inversées = erreur affichée et aucune période.
- `src/data/<domaine>.ts` : façade `server-only` qui réexporte l'implémentation PostgreSQL `<domaine>.db.ts` (Drizzle) typée par le contrat ; jamais d'accès à l'environnement au chargement d'un module (`next build` importe les pages sans `.env`). Plus de mode sans base (décision du 2026-09-15) : les fixtures ne servent qu'au seed et aux tests. Commandes : `orders.db.ts` (une commande = une requête, lignes agrégées en JSON ; recherche sur les colonnes calculées `search_text` / `phone_digits` avec index trigramme `pg_trgm`, migration 0006 ; page et total lus en parallèle ; écritures conditionnelles) et `orders-aggregates.db.ts` (KPI, séries, jours de tournée, produits phares, compteurs du personnel ou d'une personne, chiffres de l'annuaire entier ou restreint à un client ou une communauté, compteur fidélité et date de la dernière catégorie « fidèle » par fonctions de fenêtre, membres par communauté, historique des atteintes `getCustomerTierEvents`, agrégés par la base, mis en forme par les règles pures `statsFromTotals`, `fillSeries`, `rankProducts`, `loyaltyFromCount`, `tierFromReachedAt`). Messages : `messages.db.ts` (page SQL, recherche sur colonne calculée, commande jointe avec équipe, trois écritures conditionnelles ; le dashboard ne crée ni ne modifie jamais un message). Notifications : `notifications.db.ts` (lectures seules ; le dépôt est dans `updateOrderStatus`, même transaction que le statut, `INSERT … SELECT … WHERE notify_order_status`). Transverses : `session.ts` (+ `reopenSession`), `credentials.ts`, `login-attempts.ts` (table `login_attempts`, verrou de ligne ; aussi les quotas de récupération `checkQuota`), `auth-tokens.db.ts` (codes et liens : HMAC, essais, consommation conditionnelle), `mail.ts` (façade : `mail.brevo.ts` par `fetch`, `mail.file.ts` un JSON par mail ; `trySendMail` depuis `after()` pour les avis, `sendMailChecked` quand la suite dépend de l'envoi — l'invitation — et rend la cause de l'échec), `passwords.ts` (politique puis `pwned-passwords.ts`, Have I Been Pwned k-anonymity), `security-log.ts`, `privacy.ts` (export complet et anonymisation d'un client).
- `src/db/` : `schema.ts` (n'importe pas le domaine ; `test/db/schema.test.ts` aligne les enums), `client.ts` (`getDb()`, types `Db`, `DbExecutor`), `mappers.ts` (purs, testés en aller-retour), `privacy.ts` (anonymisation et purge RGPD en SQL, sans `server-only` : partagé par l'action et `scripts/rgpd-purge.ts`).
- `src/app/(dashboard)/<section>/{page,loading,actions}.tsx` ; `src/components/<domaine>/` ; `src/components/ui/` = shadcn ; `src/lib/` = utilitaires purs.
- `src/auth.ts` (Auth.js, config paresseuse), `src/proxy.ts` (accès par rôle, CSP à nonce), `src/instrumentation.ts` (env au démarrage).
- `src/app/api/v1/` : l'API de l'application FIG (`docs/api.md` ; OpenAPI construit depuis les schémas zod dans `docs/api/openapi.json` par `npm run api:openapi`, vérifié par un test, servi par `GET /api/v1/openapi.json`). Domaine `src/domain/api/` (`types.ts` bornes, `session.ts` codes et sessions, `schemas.ts` entrées strictes, `responses.ts` réponses en zod = types + OpenAPI, `views.ts` types métier → réponses, `source.ts` contrats, `openapi.ts`), devis dans `orders/quote.ts`, mail du code dans `customers/mails.ts` ; données `data/api-auth.ts` et `data/api-idempotency.ts` (+ `.db.ts`), contrats étendus (`createOrder`, `listCustomerOrders`, `createMessage`, `listCustomerMessages`, `listCustomerNotificationsPage`, `listPendingNotifications`, `markNotificationSent`, `findCustomerByEmail`, `updateCustomerProfile`, `setCustomerCommunity`, `getMemberCounts`, `getPublishedArticles`), `db/errors.ts` (violations d'unicité) ; utilitaires purs `src/lib/api/` (erreurs, lecture du corps, curseur, limitation, CORS, ETag) ; cadre des routes `src/app/api/v1/_lib/` (`apiRoute`, `requireCustomer`, `requireService`, `withIdempotency`, lectures partagées). Règles : chaque route = `export const dynamic = "force-dynamic"`, `apiRoute(METHODS, …)` et `export const OPTIONS = preflight(METHODS)` ; authentification → zod strict → relecture → règle → écriture conditionnelle → journal (`api_*`) ; jamais confiance aux prix reçus (devis recalculé, `total_mismatch`) ; `Idempotency-Key` obligatoire sur toute création ; listes par curseur (`keysetSlice`, index composites) ; réponses authentifiées `private, no-store`, lectures publiques `PUBLIC_CACHE` + ETag ; toute vue a son schéma dans `responses.ts` et son opération dans `openapi.ts`, puis `npm run api:openapi` ; codes d'erreur stables en snake_case, messages en français. Tests : `test/domain/api`, `test/lib/api`, `test/app/api/v1` (projet db : route handlers appelés avec un `Request`, `test/support/api.ts`), `e2e/api.spec.ts`. Le proxy laisse passer `/api/v1` (authentification propre). Variables : `API_SERVICE_KEY` (clé du serveur de l'application, ≥ 32, facultative : 503 sans elle), `API_CORS_ORIGINS` (origines web, facultative).
- Règles : la source renvoie des types métier, jamais des lignes Drizzle ; seule la façade importe l'implémentation db ; deux projets Vitest : `unit` (`test/domain`, `test/lib`, `test/db`, sans base) et `db` (`test/data/**`, `test/app/**` : couche données et Server Actions contre la base de TEST `test-db`, migrée et seedée par `test/support/global-setup.ts`, chaque test dans une transaction annulée par `isolateEachTest()`, `@/db/client` remplacé par `vi.mock`) ; toute requête filtrée ou agrégée en SQL est testée contre sa règle pure sur les données seedées (`test/data/orders.db.test.ts`) ; écritures sensibles à la concurrence toujours conditionnelles (`updateOrderStatus`, `assignStaff` avec `expectedStaffId`) ; `"use client"` seulement là où un hook ou un error boundary l'exige.

`dashboard/CLAUDE.md` importe `AGENTS.md`, regénéré par `next dev` : le commiter avec le travail en cours.

Git : racine du dépôt = ce dossier, branche `main`, remote `origin` = `Taek78/Dashboard_fig`. Le dossier utilisateur est lui aussi un dépôt git : ne jamais lancer `git` hors de ce dossier. CI : `.github/workflows/ci.yml` (check, audit des dépendances, Playwright), `codeql.yml` et `gitleaks.yml` hebdomadaires (faux positifs des fixtures de test dans `.gitleaks.toml`), actions épinglées par SHA avec le tag en commentaire, Dependabot hebdomadaire. Tout script qui écrit dans une base (seed, purge RGPD, restauration, base de test) passe par la garde d'hôte `src/lib/database-url.ts` : base locale seulement, sauf `SEED_ALLOW_REMOTE=1` ou `RGPD_ALLOW_REMOTE=1`. `test/data/migrations.db.test.ts` rejoue les migrations depuis la version précédente avec des lignes, sur une base jetable.

## Commandes

```bash
cd dashboard
npm run dev           # next dev (Turbopack), http://localhost:3000 ; exige .env.local (copier .env.example)
npm run check         # typecheck (next typegen puis tsc) + lint + format:check + test (unit puis db, sur la base de test), dans cet ordre
npm run build && npm run start
npm run test:e2e      # Playwright (Chromium) contre next start -p 3126 sur la base de test (migrée et seedée avant la suite) ; build préalable
npm run db:test       # docker compose up -d --wait test-db : base de TEST jetable (port 5434), requise par check et test:e2e
npm run db:generate   # schéma → migration SQL dans drizzle/ (à lire et commiter)
npm run db:migrate    # applique à DATABASE_URL
npm run db:seed       # vide et remplit la base locale (fixtures + comptes de .env.local)
npm run db:studio     # explorer les tables
npm run db:backup     # pg_dump dans %LOCALAPPDATA%\fig-backups ; db:restore -- <fichier>
npm run rgpd:purge    # durées de conservation RGPD : aperçu ; -- --apply pour écrire ; base distante refusée sans RGPD_ALLOW_REMOTE=1
npm run api:openapi   # régénère docs/api/openapi.json depuis les schémas zod (un test échoue si le fichier commité n'est plus celui du code)
npx vitest run test/domain/orders     # un dossier ou un fichier
grep -rn 'from "\.\.' src             # doit rester vide (imports relatifs interdits)
```

ESLint doit rester en version 9 (la 10 casse le plugin React de `eslint-config-next`). Prettier avec `prettier-plugin-tailwindcss` ; `drizzle/meta/` ignoré.

## Stack

- **Next.js 16 App Router**, React 19, TypeScript strict. Version à changements cassants : avant d'écrire du code framework, lire le guide dans `dashboard/node_modules/next/dist/docs/`. `PageProps<"/route">` et `LayoutProps` sont générés (`npx next typegen`) ; `searchParams` et `params` sont des `Promise` ; recherches en `AutoSubmitForm` (GET automatique, anti-rebond, `router.replace` en transition, `src/lib/search-query.ts`), `next/form` pour les autres formulaires GET ; `proxy.ts` exporte une fonction nommée `proxy`.
- **Tailwind v4** via `@tailwindcss/postcss`, thème dans `src/app/globals.css` (`@theme inline`) : trois modes par `data-theme` (light, dark, fig = crépuscule adouci inspiré de GTA VI ; **sombre par défaut**, `DEFAULT_THEME`), tokens oklch, `--success`, `--warning`, `--info`, `--fruit`, `--vegetable`, `--halo-1/2/3`, dégradé de marque ; le variant `dark:` couvre dark et fig. Jamais de couleur en dur. Utilitaires maison : `bg-gradient-brand`, `text-gradient-brand`, `card-lift` (élévation au survol), `glow-brand` (halo de coin), `skeleton-shimmer`, `page-in` (entrée de page dans `(dashboard)/template.tsx`), `float-soft` ; fond de fenêtre fixe dans `body::before/::after` (dégradé très léger, motif `public/fond/verger.svg` en masque, opacité `--decor-art-opacity`), coquille translucide (règles hors couche) ; menu rangé par groupes (`groupNavItems`), panneau mobile qui glisse depuis la gauche ; graphe d'évolution en SVG maison rendu dès le serveur (`components/metrics/comparison-chart.tsx`, géométrie `src/lib/chart.ts`), répartition par statut en barres HTML serveur ; panneau mobile de la sidebar chargé à la demande (`ui/sidebar-mobile.tsx`, `next/dynamic`) ; `cv-auto` (content-visibility) sur les cartes des longues listes ; bouton `variant="brand"` pour l'action principale d'un écran ; bordure gauche des cartes de commandes colorée par statut (`STATUS_ACCENT`) ; page de connexion en scène animée abstraite (`login-scene.tsx` pour la seule parallaxe au pointeur, animations en CSS « Connexion » de `globals.css`, tout en tokens, immobile sous `prefers-reduced-motion`).
- **shadcn/ui** style `base-nova` (primitives `@base-ui/react`, prop `render` ; un lien rendu par `<Button render>` reçoit `role="button"` : utiliser `<a className={buttonVariants(...)}>` pour un vrai lien). Composants copiés dans `src/components/ui/`, modifiés sur place.
- **Drizzle ORM** + `postgres`. Jamais `db:push` ; jamais `sql.raw()` avec une entrée utilisateur. `drizzle.config.ts` et `scripts/seed.ts` lisent `.env.local` par `process.loadEnvFile`.
- **zod v4** pour les entrées (`z.email()`, `z.iso.date()`, `.catch(undefined)` en lecture tolérante, `superRefine` + `transform` en écriture).
- **Auth.js v5** (`next-auth@beta`) : Credentials, JWT 8 h avec le rôle et l'instant d'ouverture `sat`, `trustHost` déduit d'`AUTH_URL` (obligatoire en production). Le callback `jwt` relit le compte à chaque lecture (`isSessionAlive`) : désactivé, verrouillé ou mot de passe changé depuis → session refusée, cookie effacé (le proxy laisse passer cette suppression). Mots de passe scrypt (`src/lib/password.ts`), secrets à usage unique en HMAC (`src/lib/secrets.ts`).
- Graphiques sans bibliothèque (SVG et HTML, `src/lib/chart.ts`, `src/lib/pie.ts`) ; **Playwright** pour les parcours navigateur.
- **Environnement** (`src/lib/env-schema.ts`) : `DATABASE_URL` (postgres, obligatoire), `AUTH_SECRET` ≥ 32 ; en production : `AUTH_URL` et `MAIL_TRANSPORT` (`brevo` avec `MAIL_API_KEY` et `MAIL_FROM`, ou `fichier` pour un serveur de test) ; `MAIL_FROM_NAME`, `MAIL_FILE_DIR`, `PASSWORD_BREACH_CHECK=0`, `HEALTH_TOKEN` (route de santé `/api/health` en « Authorization: Bearer », 16 caractères au moins ; sans lui la route est publique ; une seule sonde de la base par cinq secondes dans tous les cas) facultatifs ; en production, `HEALTH_TOKEN` est OBLIGATOIRE comme `AUTH_URL` et `MAIL_TRANSPORT` (`productionProblems`), à fournir au système de supervision ; la configuration Playwright le pose (valeur publique). Playwright tourne en `fichier` (`test-results/mail`) sans appel aux fuites. `API_SERVICE_KEY` (clé du serveur de l'application pour `/api/v1/service`, 32 caractères au moins, facultative : 503 sans elle ; posée par Playwright, valeur publique) et `API_CORS_ORIGINS` (origines web autorisées, séparées par des virgules) facultatives ; `AUTH_BOOTSTRAP_*` et `AUTH_MANAGER_*` ne servent qu'à `npm run db:seed` ; `TEST_DATABASE_URL` (défaut : `test-db` de `compose.yaml`) aux tests. Jamais `process.env` dans un composant client.

## Conventions

- Imports : toujours l'alias `@/` ; jamais d'import relatif ni `next/dist/…` (erreur la plus fréquente de l'auteur, la relever).
- Recherches : la barre commune (`SearchField`, inchangée), puis le panneau des filtres (`FilterTray`, surface `surface-tray`, « Réinitialiser » dans son en-tête ; cases à cocher en puces `CheckChip`) et, en dernière ligne, la zone de dates dédiée pleine largeur (`DateRangeFields`, variantes `row` dans un panneau, `zone` seule sur sa surface : période personnalisée du tableau de bord et des métriques, historique d'un client). Titres de section par `Section` (métriques, tableau de bord). Décision du client, 2026-09-17.
- Toute date affichée porte son année (`src/lib/format.ts` ; une période de la même année l'écrit une fois ; seuls les repères d'axe du graphe l'omettent). Peu de texte explicatif : ni aide sous les recherches, ni description d'en-tête qui répète le titre, ni phrase de démonstration (décision du 2026-09-16) ; garder les messages d'erreur, les états vides et les contraintes de saisie.
- Montants en centimes entiers (suffixe `Cents`), quantités en grammes ou pièces, dates en chaînes ISO, jours `AAAA-MM-JJ`, heures `HH:mm`.
- Server Actions = frontière de confiance : session → rôle → zod → relecture → règle → écriture → journal → revalidation → `ActionResult`. `verifySession` est mémorisée par requête (`cache()` de React) : layout et page ne déchiffrent le jeton qu'une fois. Jamais confiance aux identifiants, rôles, prix, quantités ou statuts reçus du client.
- CSP à nonce et `'strict-dynamic'` : un `loading.tsx` n'importe jamais un module qui contient un composant client, même s'il n'en utilise qu'un export serveur. Next écrirait le script de ce composant dans le `<head>` sans nonce, et la CSP le bloquerait au chargement complet (console). Les squelettes vivent dans leur propre module (`orders-filters-skeleton.tsx`) ; `e2e/csp.spec.ts` recharge chaque section et échoue sur toute violation.
- Logique pure isolée et testée ; les composants ne font que brancher. Un composant client n'importe jamais un module `schemas.ts` (il embarquerait zod dans le navigateur) : les bornes de saisie vivent dans `types.ts`.
- Listes de valeurs : `XXX as const` + `Record<Xxx, string>` de libellés français ; l'enum Postgres reprend les mêmes clés.
- Responsive : mobile d'abord, priorité au desktop ; chaque écran complet à 400 px et à 768 px (sidebar ouverte et repliée) sans défilement horizontal. Accessibilité automatisée : `e2e/accessibilite.spec.ts` (axe-core, WCAG 2.x A et AA et bonnes pratiques) analyse les pages publiques et les écrans principaux ; seules les violations « serious » et « critical » font échouer la suite, les autres sont jointes au rapport ; `aria-pressed` n'est permis que sur un bouton (un lien qui marque l'état courant porte `aria-current`). Dans les pages, la disposition suit la largeur de la **zone de contenu** (`@container/main` du layout), jamais celle de la fenêtre : `@xl/main:` (≈ ancien `sm:`), `@2xl/main:` (≈ `md:`), `@4xl/main:` (≈ `lg:`), `@5xl/main:` (≈ `xl:`). Les préfixes de fenêtre (`md:`…) restent réservés à la coquille (sidebar, bandeau, layout) et à ce qui s'affiche hors de la zone (connexion, portails).
- Suppressions et annulations confirmées côté serveur (mot `SUPPRIMER`, motif d'annulation).
- Motifs d'annulation : stock, livraison, **client** (« Annulée par le client », posé par l'API quand la personne annule depuis l'application, en préparation seulement ; l'équipe peut aussi le choisir), autre avec précision. `Order.paymentReference` (migration 0016) : référence du paiement transmise par l'application à la création, jamais posée par le dashboard.
- RGPD (`docs/rgpd.md`) : toute nouvelle donnée personnelle entre dans l'inventaire avec sa durée de conservation. Un client n'est jamais supprimé (ses commandes sont conservées pour la comptabilité, pseudonymes) mais anonymisé (`anonymized_at`, migration 0007, mot `ANONYMISER`), et jamais tant qu'une commande est en préparation ou expédiée (`open_orders`). Export (`GET /clients/[id]/export`, `no-store`, refusé si `Sec-Fetch-Site` vient d'un autre site) et anonymisation réservés à l'administrateur (`canHandlePrivacyRequest`), journalisés avec le seul identifiant ; ces preuves ne sont jamais purgées. Durées (hypothèses, question 18) dans `domain/privacy/retention.ts`, appliquées par `npm run rgpd:purge` : pas de `--apply` planifié avant les réponses aux questions 14, 18 et 19. Un champ vidé par l'anonymisation (téléphone) ne s'affiche pas en lien ; l'export n'inclut jamais les noms de l'équipe. Les messages « Nous contacter » sont des données personnelles : ils entrent dans l'export (avec la liste de leurs pièces jointes) et sont **supprimés** à l'anonymisation, pièces jointes comprises (cascade) ; l'export ne reprend ni le nom du traitant ni les marques internes de tri (épingle, « important »). Depuis la migration 0009, l'anonymisation efface aussi la rue, le code de parrainage, le parrain et les trois autorisations du client, la rue de livraison de ses commandes, et supprime ses notifications ; l'export reprend tout cela (catégorie et historique compris) mais ne nomme ni parrain ni filleuls (un nombre seulement).
- Listes longues : la liste des commandes est paginée PAR LA BASE (`getOrdersPage` : filtres et recherche en SQL, COUNT puis LIMIT/OFFSET, bornes de `pageWindow`), les plus récentes d'abord, 40 par page (`?page=`). Aucun écran ne charge un historique non borné : tableau de bord et métriques lisent des agrégats (`getOrderStats`, `getOrderSeries`, `getTopProducts`), les 10 commandes en préparation les plus proches (`getOrders(filtres, { limit })`) et leur nombre (`countOrders`) ; le personnel `getStaffWorkSummaries`, la fiche personnel `getStaffWorkSummary` et une page d'historique ; la fiche client et la fiche communauté `getDirectoryStats({ customerId | communityId })` et une page de commandes (`?page=`, ancre `#historique` ; fiche client restreinte à une période de livraison `?du=&au=`, `parseCustomerHistoryPeriod`, chiffres clés toujours sur tout l'historique) ; les raccourcis des 7 derniers jours de `/commandes` `getDeliveryDayCounts` ; les métriques `countComplaints(range)` (messages) et `getSignupStats(range)` (clients), chacune avec sa règle pure testée en parité. Toute nouvelle lecture est paginée, bornée ou agrégée. Frontend (audit mesuré du 2026-09-17) : les liens des cartes de liste (commandes, clients, communautés, messages, personnel, catalogue, articles, `CustomerNameLink`) sont des `HoverPrefetchLink` (`components/ui/hover-prefetch-link.tsx` : préchargement au survol seulement, jamais à l'entrée dans la fenêtre ; les liens de la sidebar gardent le `Link` ordinaire) ; chaque carte de commande est dans sa propre frontière `Suspense` (`OrdersCards`), pour que le navigateur peigne entre deux cartes pendant l'hydratation. Repères sur le serveur construit : TTFB 40 à 70 ms, CLS nul, 210 à 240 Ko de JavaScript compressé par page dont 204 Ko communs ; ne pas ajouter de récupération de données côté client, de bibliothèque de graphiques ni de script tiers.
- Recherche et filtres : `/commandes` (liste ET tournée depuis le 2026-09-16) utilise `OrdersFilters` et `parseOrderFilters` (`?q=&statut=&du=&au=&preparateur=&livreur=`, `aucun` = non affecté, `?date=` encore lu comme un seul jour) ; la recherche libre est la règle pure `matchesOrderQuery` (référence, nom, e-mail, téléphone, ville, code postal), reproduite par `orders.db.ts` sur des colonnes calculées par la base (fonction SQL `fig_normalize`, même règle que `normalize()`) avec un motif LIKE échappé (`containsPattern`), et testée contre elle ; `orderFiltersQuery` réécrit l'URL pour la pagination et les raccourcis des 7 derniers jours (`DeliveryDayShortcuts`, chaque jour avec son nombre de livraisons). L'avancement segmenté par statut reste au tableau de bord (`tourProgressFromCounts`). Les jours `AAAA-MM-JJ` se calculent avec `src/lib/days.ts`.
- Dates « du / au » (décision du 2026-09-16, tous les écrans : commandes, messages, historiques des fiches client et personnel, période personnalisée du tableau de bord et des métriques) : les deux champs sont UN filtre, groupés dans `DateRangeFields` ; `parsePeriodInput` (orders/schemas) lit l'URL par `readDateRange` : une seule date = ce jour-là, `du` ≤ `au` accepté (même jour compris), inversées = erreur rouge (`role="alert"`) et aucune période appliquée, jamais d'échange silencieux. Une période valide sans résultat s'annonce par le bandeau bleu `PeriodEmptyNotice` (token `--info`, `role="status"`) avec un lien vers toutes les dates, autres filtres gardés. Les pages passent la saisie brute (`DateRangeInput`) au formulaire et la période effective à la source. Calendrier maison sur tablette et PC (`DatePickerButton`, `src/lib/calendar.ts` : jours voisins grisés, ouverture sur le mois de la date saisie, sinon de l'autre borne, sinon le mois actuel ; il écrit le champ natif, qui reste la seule valeur du formulaire) ; sur téléphone (fenêtre < `md`), sélecteur natif. Tableau de bord et métriques (2026-09-17) : la liste « Période » finit par « Période personnalisée » (`CUSTOM_PERIOD`, `?periode=personnalisee`) et la zone de dates n'est MONTÉE que pour ce choix (`PeriodChooser`, client : montée dès le choix, démontée pour une période prédéfinie, ses champs ne partent alors pas dans l'URL) ; des dates dans l'URL l'ouvrent aussi (anciens liens) ; sans plage effective, la période prédéfinie de l'URL ou le défaut reste affichée ; `periodParams` rejoue le choix dans les liens HT / TTC ; bouton « Afficher » et légende « Période affichée » sous la zone.
- Équipe et remises : chaque commande porte `preparer`, `driver` (affectés par `assignOrderStaff`, liste déroulante qui écrit dès le choix), `community`, `discount` et `deliveryFeeCents` (calculés par le DEVIS de l'API (`orders/quote.ts`) à la création par l'application, jamais modifiés par les écrans du dashboard : la commande porte ce qui a été payé ; `totalCents` = lignes − remise + frais). Une personne supprimée libère ses commandes (`SET NULL`) ; préférer `active = false`. Règles du client (2026-09-16) : **fidélité** = compteur CUMULÉ (`loyaltyCount` : une annulée ne compte pas et ne remet pas à zéro, la commande remisée repart de zéro), membres de communauté compris ; à 8, la prochaine commande est à −15 % et le client est **fidèle** deux mois (`customerTier`, `loyalTierEvents`), sinon **basique** ; rien n'est stocké, tout se déduit des commandes (règle pure = requête SQL, testées entre elles). **La meilleure remise l'emporte** (`bestDiscount`) : la fidélité prête remplace la remise de communauté, jamais de cumul. **Remise de communauté** déduite du nombre de membres (`communityDiscountPercent` : 0 jusqu'à 3, 5 % de 4 à 9, 10 % dès 10 ; colonne `discount_percent` supprimée), **livraison offerte** à toute communauté (contrainte `orders_community_delivery_free`) ; **frais** au barème du panier avant remise (`deliveryFeeCents` : 4,90 / 3,90 / 2,90 / 1,90 € sous 5, dès 5, 10, 20 €) ; **créneau d'une heure pile entre 10:00 et 20:00** (`slotEndFor`, `isDeliverySlot`, contraintes `orders_slot_one_hour` et `orders_slot_hours`). L'horaire de retrait d'une communauté est choisi à chaque commande dans l'application (`deliverySlot`) : une communauté n'a pas d'heure fixe. **Notifications d'état** : `changeOrderStatus` compose le texte (`orderStatusNotification`, jamais le nom de la personne) et la source le dépose dans `customer_notifications` seulement si `customers.notify_order_status` ; le dashboard n'envoie rien, le serveur de l'application lit la file par l'API (`/api/v1/service/notifications`, clé de service) et accuse l'envoi (question 23). **Clients** : rue `addressLine` (null si inconnue), trois autorisations `consents` (recueillies et datées par l'API, jamais modifiées par les écrans), `referralCode` « Nom#0000 » unique et `referredBy` (attribués par l'API à l'inscription : `signupCustomer`) ; filleuls par `getCustomerReferrals`, fiche seulement. Statuts : quatre valeurs, en préparation, expédiée (`delivering`), livrée, annulée ; ni « confirmée » ni « en attente » (décisions du 2026-09-15, migrations 0003 et 0005). **Changement de statut libre** (décision du 2026-09-17) : plus de règle d'étape, `canTransition` n'exclut que le statut courant et `allowedTransitions` renvoie les trois autres (livrée directement, retour en préparation, reprise d'une annulée) ; l'annulation exige toujours son motif. Liste déroulante `OrderStatusSelect` (client, cartes et fiche) qui écrit dès le choix (FormData composé, action dans une transition), icône et couleur du statut choisi à côté (`STATUS_ICONS`, `STATUS_ICON_TONE`, `STATUS_SELECT_TONE` : ambre, bleu, vert, rouge), « Annulée » ouvrant le motif avant « Confirmer l'annulation » ; à chaque changement la notification d'état est déposée pour le client qui l'a autorisée, retour en préparation compris, SAUF si la case « Notifier le client » (cochée par défaut ; champ `notify`, « 1 » cochée, « 0 » ou absent décochée) a été décochée : rien n'est déposé. Sous la confirmation, une seconde ligne courte dit « Client notifié » (badge vert ; `notified` renvoyé par l'action = case cochée ET autorisation relue sur la commande, `StatusChangeResult`) ou « Client non notifié » (gris) ; le motif d'une annulation est dit « communiqué au client » ou seulement « enregistré » selon le cas. Elle est décochée par défaut dès que la commande est passée une fois par « livrée » (`Order.wasDelivered`, EXISTS sur `order_events` relu avec la commande, y compris à l'instant où elle devient livrée) et désactivée, libellée « Notifications non autorisées par le client », quand le client n'a pas donné l'autorisation (`Order.customer.notifyOrderStatus`, relu avec la commande ; la fiche n'interroge plus le client à part). Chaque commande et chaque client affichent leur type (tokens `--individual` et `--community`). **Type de commande** (2026-09-17) : une commande portée par une communauté est une commande GROUPÉE de type « communauté » (`orderKindOf`, déduit, jamais stocké) : carte, tableau et détail nomment la communauté puis l'**interlocuteur** (la personne qui a commandé, garante, livrée pour tous), bande et badge en couleur communauté ; sinon commande d'un particulier, en bleu ; filtre `?type=particulier|communaute` (`kind` dans `OrderFilters`, `whereFor` sur `community_id`, commutateur `TypeSwitch` partagé avec `/clients`). Côté personnes : une personne est TOUJOURS « Particulier », avec à côté le badge « Communauté » et le nom du groupe si elle en est membre (`CustomerTypeLabels`) ; seul un groupe a le type « Communauté » (`ClientTypeLabel type="communaute"`) ; une personne reste en couleur « particulier » (avatar, bande de commande), seul le badge d'appartenance est en couleur communauté ; la position « particuliers » de `/clients` inclut les membres, la position « communautés » ne montre que les groupes.
- Messages (`/messages`) : boîte de réception des demandes « Nous contacter », **déposées par l'application FIG par l'API** (`POST /api/v1/messages`, pièces jointes hébergées par l'application) ; le dashboard ne pose que trois marques, toutes en écriture conditionnelle sur l'état relu (`status` non traité / traité, `pinned_at` épinglé en tête, `important`), réservées à l'admin et au gestionnaire (`canHandleMessages`). Objet parmi six valeurs, commande associée facultative, dix pièces jointes au plus : la limite et la liste blanche de formats (PDF et images, ni vidéo ni audio) sont tenues par la BASE (enum `attachment_content_type`, `position` bornée), pas par un écran, et redites par le schéma zod de l'API. Les fichiers sont hébergés par l'application (question 22) : le dashboard n'en stocke que les métadonnées et l'URL. Liste triée épinglés d'abord puis les plus récents, carte avec les deux premières lignes (`messagePreview`), fiche complète précédée de la `CustomerCard` du client.
- Sections : `/commandes` sert de tournée (page d'accueil du livreur ; carte avec le créneau en grand, Appeler et Itinéraire, nom du client en lien ; `/livraisons` redirige vers `/commandes` dans `next.config.ts`) ; sous son compteur, la barre d'avancement de TOUTES les commandes listées (`getOrderStatusCounts` : même WHERE que la liste, GROUP BY statut ; `TourProgress`) ; les raccourcis des 7 derniers jours sont dans la zone de dates de ses filtres, « Aujourd'hui » en tête et en évidence (`DeliveryDayShortcuts`) ; `/` affiche en tête `StaffShortageAlert`, puis, sans commande sur la période, un état vide « Aucune commande » bien visible (`Empty`) à la place de la barre (`unavailableRoles` : aucun préparateur ou aucun livreur actif « disponible », quelle que soit la raison de l'absence) ; les listes d'affectation portent une gommette 🟢 présent / 🔴 absent dans le texte des options (`staffOptionLabel` : un `<select>` natif n'accepte ni icône ni couleur, surtout sur téléphone), grise en lecture sur une commande livrée ou annulée (`OrderTeam`) ; la carte de commande n'écrit que la remise entre parenthèses près du total (frais et sous-total dans le détail) ; `/catalogue` : statut de vente déduit (`products/status.ts`, `productSaleStatus` : masqué, indisponible, rupture de stock, en vente) en pastille sur l'image, paramètre global « laisser en vente à stock 0 » (table `catalog_settings`, migration 0012, `saveCatalogSettings`) en case entre la recherche et la grille ; le nom d'un client est PARTOUT un lien vers sa fiche (`CustomerNameLink`, cartes, tableaux, détail, messages) ; `/messages` : sur la carte la commande jointe précède le texte, les pièces jointes sont en violet (`AttachmentBadge`, token `--attachment`) ; `/metriques` : « Réclamations » = messages de `CLAIM_SUBJECTS` reçus sur la période (`countComplaints`), section « Clients » avec nouveaux inscrits et parrainages (`getSignupStats`), commandes en tête avec CA, panier et acheteurs ; `/personnel` (admin, gestionnaire, lecture ; recherche `?q=&type=&dispo=&creneau=&jour=&presence=`, règles pures `searchStaff` / `matchesStaffQuery`), `/clients` en recherche commune (`?q=&type=tous|particuliers|communautes&tri=<nom|commandes|montant|recent|anciennete|membres>&sens=<croissant|decroissant>&page=` (sens écrit seulement s'il diffère du naturel ; `anciennete` = date d'inscription du client ou de création de la communauté, naturellement du plus récent au plus ancien, affichée sur la carte ; l'ancienne forme `tri=<critère>-<sens>` reste lue), commutateur de type en boutons radio colorés, critère dans une liste déroulante (`sortOptions`) et sens par un bouton icône à côté (`SortOrderToggle` : A / Z ou 1 / 9, flèche bas = croissant, rotation de 200 ms), `membres` seulement en position communautés (qui ne montre que les groupes), règles pures de `customers/directory.ts`, grandes cartes avec bouton « Voir le détail », fond nuancé de `--individual` (client) ou `--community` (communauté) ; carte = adresse, autorisations en pastilles, catégorie en étoiles (`TierBadge`, token `--loyal`) ; fiche en lignes pleine largeur = coordonnées, chiffres, notes ; autorisations en trois colonnes ; parrainage (code et parrain, puis filleuls) ; fidélité (compteur, puis historique des statuts) ; historique des commandes). Fiche personnel : formulaire en haut (`#modifier`), historique filtrable (`?q=&role=&statut=&du=&au=`) ; cartes avec modifier, dupliquer (`/personnel/nouveau?depuis=`) et supprimer pour admin et gestionnaire. Métriques rangées en sections (`MetricsSection`), camemberts pleins (`RatioPie`, géométrie `src/lib/pie.ts`). Un champ répété (cases à cocher `workDays`) se lit avec `formData.getAll` avant zod.
- Comptes et récupération (décision du client, 2026-09-17) : un compte se crée SANS mot de passe (`createUser` avec `passwordHash: null`) et reste EN ATTENTE D'ACTIVATION tant que la personne n'a pas choisi le sien par le lien d'invitation (48 h, `/connexion/invitation?jeton=`) : badge « En attente d'activation », carte translucide bordée de pointillés ambre (`invitationState` = `pending`, sur `ManagedUser.invitationExpiresAt` joint par la source ; l'horloge est lue par `listUsersWithInvitations`, jamais dans la page), « Renvoyer l'invitation » et « Annuler l'invitation » (`cancelInvitation`, confirmation sans mot : le compte jamais activé est supprimé, journal `invitation_cancelled`, avis `invitationCancelledMail` à la personne SI son invitation était partie) à la place de « Désactiver » et « Supprimer ». **L'envoi de l'invitation est attendu** (2026-09-18, `sendMailChecked` au lieu d'`after`) : l'écran ne promet jamais un mail qu'il n'a pas vu partir. Réussite, la carte porte « Invitation envoyée le … » ; échec, un bloc rouge donne la cause et le geste (`domain/mail/failure.ts`, `MailFailureReason` = enum Postgres `mail_failure_reason`), le message du formulaire passe en `status: "warning"` (ambre, `ActionStatus`), et l'issue est écrite sur le compte (`users.invitation_mail_sent_at` / `invitation_mail_failed_at` + `invitation_mail_error`, migration 0017, remise à zéro par un envoi réussi ; journal `invitation_mail_failed`). Passé le délai, carte rouge « Invitation expirée » (`expired`) et balayage `notifyExpiredInvitations` (`data/invitation-expiry.ts` : `expireInvitations`, UNE écriture conditionnelle qui pose `users.invitation_expired_at` = expiration du lien notifié, migration 0015, une seule fois par lien, un lien renvoyé depuis redevenant notifiable) qui envoie `invitationExpiredMail` à la personne et `adminInvitationExpiredMail` à chaque administrateur actif ; il tourne par `instrumentation.ts` (une minute après le démarrage puis toutes les quinze minutes, jamais au build) et après la réponse de la page Comptes (`after`). À l'activation (lien accepté, ou premier mot de passe posé par le dépannage), `accountActivatedMail` part à la personne (adresse de connexion, identifiant, rôle, administrateur à contacter, jamais le mot de passe) et `adminAccountActivatedMail` aux administrateurs. `activeAdmins` exclut un administrateur invité sans mot de passe (ni dernier administrateur, ni alerté, ni nommé comme contact). « Nouveau mot de passe » n'est qu'un dépannage. Un compte porte un PRÉNOM et un NOM (migration 0014, `first_name` / `last_name`) : le couple est unique sans casse ni accent (`fig_normalize`, index `users_full_name_normalized_idx`), l'affichage `name` = « Prénom Nom » est dérivé par le mapper (`fullName`) ; « Adresse e-mail oubliée » demande le NOM seul (`findUsersByLastName`, index `users_last_name_normalized_idx` : chaque compte actif qui le porte reçoit son propre rappel). L'e-mail d'un compte se lit sur sa carte (champ en lecture seule) et ne se modifie jamais. Suppression définitive (`deleteAccount`, mot `SUPPRIMER` en toute casse, `deleteUserSchema`) : jamais soi-même, jamais le dernier administrateur actif (`isLastActiveAdmin`, qui n'est pas non plus désactivable ni rétrogradable ; l'écran cache ces boutons et le dit) ; d'autres administrateurs peuvent être créés puis supprimés tant qu'il en reste un ; `auth_tokens` supprimés en cascade, sessions closes à la requête suivante, l'historique des commandes garde le nom écrit ; journal `account_deleted`. Désactiver ou supprimer un compte envoie un mail à la personne après la réponse (`accountDeactivatedMail`, `accountDeletedMail` : ton professionnel, date, ce que cela change, ADRESSE de l'administrateur à contacter, jamais son nom, par `adminContact`, commun à tous les avis à une personne (demande du 2026-09-18 : le nom d'un membre de l'équipe ne sort pas du back-office ; les avis ENTRE administrateurs gardent le nom de celui qui a agi, traçabilité interne), signature « Bien cordialement, L'équipe FIG » ; la suppression précise qu'une demande peut être adressée depuis l'ancienne adresse et qu'un nouveau compte peut y être créé, sur invitation, seul un administrateur étant habilité à créer un compte et à envoyer l'invitation). « Mot de passe oublié » (`/connexion/recuperation`) : code à six chiffres (5 min, 5 essais, HMAC en `auth_tokens`), réponse et redirection identiques que l'adresse existe ou non, quotas 3 par quart d'heure par sujet et 10 par IP (`RECOVERY_*_POLICY`), mails après la réponse (`after`, `trySendMail`). Alertes : la personne reçoit le code puis l'avis de changement, chacun avec un lien « Ce n'était pas moi » (24 h, `/connexion/verrouiller?jeton=`, bouton POST : désactive, `revokeSessions`, prévient) ; les administrateurs actifs (`activeAdmins`) reçoivent le changement effectif, les cinq codes erronés et le verrouillage. Toute écriture de mot de passe passe par `findPasswordProblem` (règles pures de `password-policy.ts` puis Have I Been Pwned) et `setPassword(id, hash, changedAt)` avec l'horloge de l'application ; changer son propre mot de passe rouvre sa session (`reopenSession`). Mails en texte brut composés par `domain/auth/mails.ts`, jamais un secret dans le journal (`recovery_*`, `password_recovered`, `account_locked_by_owner`, `invitation_*`, `email_reminder_requested`, `mail_failed`). Écrans publics dans `LoginShell` (scène partagée), champ `PasswordField` avec jauge dans tous les formulaires de mot de passe.

## Déroulement d'une tâche

1. Inspecter l'état Git et les fichiers concernés ; ne jamais inventer l'état d'un fichier, d'une API ou d'une commande non inspectée.
2. Reformuler l'objectif, présenter diagnostic et plan quand plusieurs lectures sont possibles.
3. Appliquer, tests compris.
4. `npm run check`, build et Playwright si l'écran change, vérification en `next start` si le comportement serveur change.
5. Résumer fichiers modifiés, validations, risques ; mettre à jour docs et glossaire.

## Sécurité

- Ne jamais afficher, journaliser, commiter ou copier un secret ou un mot de passe.
- Ne jamais exécuter de migration, de seed ou de restauration sur une base distante sans confirmation ; présenter le SQL généré et signaler les opérations destructrices.
- Toute action d'administration vérifie authentification et autorisation côté serveur.

## Sources de vérité

En cas de conflit : 1. la demande explicite de l'utilisateur ; 2. la sécurité et l'intégrité des données ; 3. le code et la configuration réellement présents ; 4. la documentation locale de la version installée ; 5. ce fichier.
